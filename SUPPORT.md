# 支援版本矩陣（Support Matrix）

本檔定義 ZeroTrustAuth 的**目標支援範圍（政策）**，並區分：
- **目標支援**：承諾支援的 Minecraft 版本（依下列政策）。
- **目前已建置／CI 驗證**：現階段 `mods.yml` 實際建置、`mc-server-test.yml` 實機測試者（旗艦子集）。

> ⚠️ Minecraft 各版本的 Mod/伺服器 API **互不相容**：一個建置產物通常只對應**一個版本（或同一小版本線）**。因此「目標矩陣」中的每個版本都需各自建置，並非單一 jar 通吃。

---

## 政策

### Forge（舊版線）
- 範圍：**1.7.10 → 1.20.1**，但**僅限下列採用率高、Forge 支援成熟的世代**；其餘世代不支援。
- 規則：**除 1.20.1 外，每個受支援大版本只取該大版本的「最新小版本」**；
  並**排除** Forge 支援薄弱／採用率低的世代：**1.9、1.10、1.11、1.13、1.14、1.15、1.17**。
- 具體目標版本（**7 個**）：
  `1.7.10` · `1.8.9` · `1.12.2` · `1.16.5` · `1.18.2` · `1.19.4` · **`1.20.1`**
- `1.20.1` 為與 NeoForge 的交界版（Forge 與 NeoForge 皆支援）。

### NeoForge（現代線）
- 範圍：**1.20.1 起**（含後續所有大版本）。
- 規則：`1.20.1` ＋ **每個大版本的「最新小版本」**（例：`1.20.6`、最新 `1.21.x`、最新 `1.22.x`…）。

### Fabric
- 範圍：**`1.20.1` → `26.1` 的所有版本**（逐版支援，不限最新小版本）。

### Paper / Spigot
- 範圍：**`1.20.1` → `26.1` 的所有版本**。
- 期限：每個版本支援至 **Paper 官方停止更新該版本後「半年」** 為止。

> 註：`26.1` 為上界（依新版號制）。實際支援以該版本發布且各平台 API 可用為準。

---

## 目前已建置 / CI 驗證（旗艦子集）

| 平台 | 目前建置版本 | Java | CI |
|--|--|--|--|
| Paper | 26.1.2 | 25 | ✅ 真機伺服器測試（`mc-server-test.yml`）|
| Fabric | 26.1 | 25 | ✅ 建置（`mods.yml`）|
| NeoForge | 26.1.2 | 25 | ✅ 建置（`mods.yml`）|
| Forge | **1.20.1（舊版旗艦）** | 17 | ✅ 建置（`mods.yml`，獨立 pinned Gradle 8 job）|

落差說明：
- ✅ 現代線（Paper/Fabric/NeoForge）已對齊政策**旗艦頂版 26.1**：**Paper 26.1.2** 經 `mc-server-test.yml` **真機伺服器測試**（啟動自檢通過、`authkey enroll` 可用）；**Fabric 26.1**、**NeoForge 26.1.2** 為 CI **建置**驗證（非真機執行）。
- ✅ Forge 已對齊政策**旗艦頂版 1.20.1**（原 1.19.2 已淘汰），以 **pinned Gradle 8.10.2** 獨立 job 建置（ForgeGradle 6 無法在 Gradle 9 執行）。
- 無待補落差。

---

## 如何把某個版本加入「已建置」

1. 設定版本：`gradle.properties`（`minecraftVersion` / loader 版本）＋ 該平台 metadata
   （`fabric.mod.json`、`neoforge.mods.toml`、`mods.toml`、Paper `plugin.yml` 的 `api-version`）。
2. 撰寫／調整該版本的 `PlatformAdapter`：**事件、網路、權限 API 會因版本而異**
   （例：1.20.5+ 用 `CustomPacketPayload`，之前用 `SimpleChannel`；1.20.1 是 Forge/NeoForge 分家點）。
3. 加入 `mods.yml` 的 build matrix（並依該版本所需 Java 設定 toolchain）。
4. CI 綠燈後即列入上表。

### 工程量提醒（Forge 舊版線）
受支援的 **7 個版本**（`1.7.10` · `1.8.9` · `1.12.2` · `1.16.5` · `1.18.2` · `1.19.4` · `1.20.1`）
仍橫跨多次**重大且不相容**的 API 變革（1.7→1.8、1.12→1.16 含「flattening」、1.18 起 Java 17、
1.20.1 NeoForge 分家）。各世代幾乎是**獨立的平台適配實作**，故舊版線仍是矩陣中工程量最大者，
建議分階段逐版推進。已**排除**採用率低／支援薄弱的世代（1.9–1.11、1.13–1.15、1.17）以縮減範圍。
